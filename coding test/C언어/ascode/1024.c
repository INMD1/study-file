#include <stdio.h>


int main() {
	double a, b, c, d, e, f, g ;
	scanf("%lf""%lf""%lf""%lf""%lf""%lf""%lf", &a, &b, &c, &d, &e, &f, &g);
	printf("%.2f", (a + b + c + d + e + f + g) / 7);

    return 0;
}